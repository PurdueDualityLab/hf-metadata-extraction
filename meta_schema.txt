model id
tags
datasets
language
license
model task
parameters
*evaluation results

*github? 
*paper?
carbon emission
*hyper parameters
    (patch size, steps, epochs, batch size, dropout prob, resolution(input size))
hardware
tensor type
download rates
*bias (summarize in one sentence)
does format support safetensor library (yes, no)


include type (url, dict, string)

find a way to separate through different subheadings

if eval is image provide url


mark some metadata as required
    dataset
    architecture

if required value but does not exist return null
if not required and does not exist, remove from table

get metadata from modelcard template
https://github.com/huggingface/huggingface_hub/blob/main/src/huggingface_hub/templates/modelcard_template.md


change temperature for consistency
https://platform.openai.com/docs/api-reference/completions/create

if model says im not sure , you could ask are you sure

learn prompt techniques through prompt engineering
https://www.promptingguide.ai/techniques
zero shot, few shot prompting



pipeline tags
finetuning
graphs?